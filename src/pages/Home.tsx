const Home = () => {
  return (
    <div className="bg-white p-8 rounded-lg shadow font-sans">
      <h2 className="text-2xl font-semibold mb-4 text-gray-800">Welcome to DtoM!</h2>
      <p className="text-gray-600">
        Vite + React + TypeScript + Tailwind CSS (v4) setup is completed successfully.
      </p>
    </div>
  );
};

export default Home;
